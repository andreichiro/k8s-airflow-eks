apiVersion: v1
kind: Service
metadata:
  name: airflow-elb-web
  namespace: airflow
  labels:
    role: webserver
  annotations:
spec:
  selector:
    role: webserver
  ports:
    - name: web
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer